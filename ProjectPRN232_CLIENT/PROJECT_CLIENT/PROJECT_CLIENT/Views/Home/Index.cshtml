@model IEnumerable<PROJECT_CLIENT.DTO.ArticleDTO>
@{
    var categories = ViewBag.Categories as IEnumerable<PROJECT_CLIENT.DTO.CategoryDTO>;
    var currentCategory = Context.Request.Query["categoryName"].ToString();
}


<style>
    .text-purple {
        color: #6f42c1;
    }

    .category-scroll {
        display: flex;
        overflow-x: auto;
        white-space: nowrap;
        padding: 10px 0;
        border-bottom: 1px solid #ccc;
        margin-bottom: 20px;
    }

        .category-scroll a {
            display: inline-block;
            margin-right: 15px;
            text-decoration: none;
            color: #6f42c1;
            padding: 6px 12px;
            border-radius: 5px;
            white-space: nowrap;
            font-weight: 500;
            border: 1px solid transparent;
            transition: background-color 0.3s, color 0.3s;
        }

            .category-scroll a:hover {
                background-color: #f3e9fd;
                color: #6f42c1;
            }

            .category-scroll a.active {
                background-color: #6f42c1;
                color: #fff !important;
            }

    .search-form {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 10px;
        margin-bottom: 20px;
    }

    .card-title {
        font-size: 1.1rem;
        font-weight: 600;
        color: #333;
        min-height: 3em;
    }

    .card-text {
        font-size: 0.95rem;
        color: #555;
    }

    .card-img-top {
        height: 180px;
        object-fit: cover;
    }

    .card {
        transition: transform 0.2s ease-in-out;
    }

        .card:hover {
            transform: scale(1.02);
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.15);
        }

    .btn-purple {
        background-color: #6f42c1;
        border-color: #6f42c1;
        color: white;
    }

        .btn-purple:hover {
            background-color: #59329d;
            border-color: #59329d;
        }

    .form-select, .form-control {
        border-radius: 6px;
    }
</style>
<div class="d-flex justify-content-end mb-3">
    @if (User.Identity.IsAuthenticated)
    {
        var role = User.Claims.FirstOrDefault(c => c.Type == "http://schemas.microsoft.com/ws/2008/06/identity/claims/role")?.Value;

@if (role == "Admin")
{
    <a href="/AdminClient/AccountList" class="btn btn-outline-primary me-2">⚙️ Quản lý</a>
}
else if (role == "Staff")
{
    <a href="/Staff" class="btn btn-outline-primary me-2">⚙️ Quản lý</a>
}
else if (role == "Writer")
{
    <a href="/Writer/MyArticles" class="btn btn-outline-primary me-2">✍️ Viết bài</a>
}
        <form asp-controller="Login" asp-action="Logout" method="post" class="ms-2">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-outline-danger">🚪 Đăng xuất</button>
        </form>
    }
    else
    {
        <a href="/Login" class="btn btn-success">🔐 Đăng nhập</a>
    }
</div>




<div class="search-form">
    <form asp-action="Index" method="get">
        <input type="text" name="searchTitle" class="form-control mb-3" placeholder="🔎 Tìm kiếm bài viết..."
               value="@Context.Request.Query["searchTitle"]" />

        <div class="row">
            <div class="col-md-4">
                <label>Thời gian</label>
                <select name="time" class="form-select">
                    <option value="">Tất cả</option>
                    <option value="24h">24 giờ qua</option>
                    <option value="7d">7 ngày qua</option>
                    <option value="30d">30 ngày qua</option>
                </select>
            </div>
             <div class="col-md-4">
                <label>Chuyên mục</label>
                <select name="categoryName" class="form-select">
                    <option value="">Tất cả</option>
                    @foreach (var cat in categories)
                    {
                        <option value="@cat.CategoryName" selected="@(currentCategory == cat.CategoryName ? "selected" : null)">
                            @cat.CategoryName
                        </option>
                    }
                </select>
            </div> 
        </div>
        <button type="submit" class="btn btn-purple mt-3">Lọc bài viết</button>
    </form>
</div>
<h3 class="mt-4 mb-3 text-purple">📚 Danh sách bài viết</h3>

 <div class="category-scroll">
    <a class="@(string.IsNullOrEmpty(currentCategory) ? "active" : "")" asp-action="Index">Tất cả</a>
    @foreach (var cat in categories)
    {
        <a class="@(currentCategory == cat.CategoryName ? "active" : "")"
           asp-action="Index" asp-route-categoryName="@cat.CategoryName">
            @cat.CategoryName
        </a>
    }
</div> 

<div class="container">
    <div class="row">
        @foreach (var article in Model)
        {
            <div class="col-md-3 mb-5">
                <div class="card h-100">
                    <img src="@article.ImagePath" class="card-img-top" alt="image" />
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@article.NewsTitle</h5>
                        <p class="card-text">@article.NewsContent.Substring(0, Math.Min(article.NewsContent.Length, 150))...</p>
                        <a asp-controller="Home" asp-action="Details" asp-route-id="@article.NewsArticleId" class="btn btn-purple mt-2">Xem chi tiết</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
